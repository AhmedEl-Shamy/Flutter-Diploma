void main() {
  String firstName = 'Ahmed'; // [A, h, m, e, d]
  String lastName = 'Mohamed';
  int age = 25;
  // print(age.toString()[1]);
  // print(firstName[3]); //int , String
  // print(firstName + ' ' + lastName + age.toString());
  // print('$firstName $lastName');
  // print('Name: $firstName $lastName, Age: ${age + 5}');

  String name = '$firstName $lastName $firstName';
  // name = 'Ahmed Mohamed Ahmed'
  // print(name.length);
  // print(name.isEmpty);
  // print(name.isNotEmpty);
  // print(name.contains('Mohamed'));
  // print(name.indexOf('Mohamed'));
  // print(name.lastIndexOf('m', 4));
  // print(name.lastIndexOf('Ahmed'));
  // print(name.replaceAll('m', 'M'));
  // print(name.replaceFirst('m', 'M'));
  // print(name.replaceRange(0, 5, 'Ali'));
  // print(name.split('Mohamed'));
  // print(name.split(' '));
  // print(name.substring(6));
  // name = name.substring(0, 6) + name.substring(14);
  // print(name.toUpperCase());
  // print(name.toLowerCase());
  // trim, trimRight, trimLeft, padRight, padLeft, startWith, startRight
  // \n => new line, \t => tap (4 spaces), \\ for backslash, \' => ', \" => "
  print("Produt\'s Price: 200\$");
  // print(name.codeUnitAt(5));








  // print();

}
